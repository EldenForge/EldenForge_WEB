# Base URL of the EldenForge API
# In dev, requests to /api/* are proxied to this target via vite.config.ts
# Set this directly if CORS is enabled on the API and you want direct calls
# VITE_API_BASE_URL=http://localhost:8000
